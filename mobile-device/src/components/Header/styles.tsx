import { StyleSheet } from 'react-native'

export default StyleSheet.create({
	headerContainer: {
		flex: 1,
		backgroundColor: '#000'
    },
    headerTitle: {
        fontSize: 26,
        color: '#fff'
    }
})
